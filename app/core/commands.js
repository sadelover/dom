// 关闭登录窗口
const CLOSE_LOGIN_WINDOW = 'login.close-login-window';
// 登录成功事件
const AFTER_LOGIN_SUCCESS = 'login.after-login-success';
// 关闭主窗口
const CLOSE_APP_WINDOW = 'app.close-app-window';
// 最小化主窗口
const MINIMIZE_APP_WINDOW = 'app.minimize-app-window';
//最大化窗口
const MAXIMIZE_APP_WINDOW = 'app.maximize-app-window';

const SPLIT_APP_WINDOW = 'app.split-app-window';

export default {
  CLOSE_LOGIN_WINDOW,
  AFTER_LOGIN_SUCCESS,
  CLOSE_APP_WINDOW,
  MINIMIZE_APP_WINDOW,
  MAXIMIZE_APP_WINDOW,
  SPLIT_APP_WINDOW
}
